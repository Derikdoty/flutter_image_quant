name: image_picker3
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  image: ^4.1.3
  flutter_colorpicker: ^1.1.0

 
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  ffigen: ^11.0.0  # Or latest version from pub.dev

  
  flutter_lints: ^5.0.0


flutter:

  
  uses-material-design: true

 
